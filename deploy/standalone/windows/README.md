# Runtime Windows

Copie `tunnara.exe` e `tunnara-server.exe` para uma pasta permanente e execute os scripts PowerShell do diretório `deploy/native/windows` como Administrador. A release do GitHub publica os executáveis assináveis separadamente.
