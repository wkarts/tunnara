global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: tunnara
    metrics_path: /metrics
    static_configs:
      - targets: ["tunnara-server:7100"]
        labels:
          service: tunnara-control-edge-relay
